server:
  context-path: /micro
  port: 8080

spring:
  cloudant:
    username: ${vcap.services.lightblue-cloudant-ff.credentials.username}
    password: ${vcap.services.lightblue-cloudant-ff.credentials.password}
    host: ${vcap.services.lightblue-cloudant-ff.credentials.host}
    protocol: https
    port: 443
    database: customers